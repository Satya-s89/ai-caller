# ai-caller — install all dependencies with:
#   cd agent && pip install -r requirements.txt
#
# Then run the whole system:
#   py run.py  (from project root)

# ── Shared utilities (install alongside agent) ─────────────────────────────
aiohttp>=3.9.0        # used by sarvam_stt.py (Sarvam AI STT API)
httpx>=0.27.0         # HTTP client for tests
python-dotenv>=1.0.0  # .env loading
