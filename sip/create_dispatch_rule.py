"""
sip/create_dispatch_rule.py
----------------------------
One-shot script: creates a LiveKit SIP dispatch rule routing inbound calls
to the telugu-voice-agent worker.

Required env vars:
    LIVEKIT_URL, LIVEKIT_API_KEY, LIVEKIT_API_SECRET

Usage:
    cd sip
    python create_dispatch_rule.py --trunk-id <TRUNK_ID>
"""

# NOTE: This script is a Stage 5 stub.
# Full implementation will be added in Stage 5.
print("Stage 5 stub — run after Stage 4 is complete.")
