# Stage 5 placeholder — reference audio for IndicF5 TTS voice cloning.
# Replace this file with a real ~5-second Telugu WAV clip (mono, 16-bit, any sample rate).
# Then update TTS_REFERENCE_TEXT in your .env to match the transcript of your clip.
# The speaker's voice in this file will be the voice the agent uses on calls.
