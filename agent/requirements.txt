# agent/requirements.txt
# Install with: pip install -r requirements.txt

# ── LiveKit Agent framework ────────────────────────────────────────────────────
livekit-agents>=1.6.0
livekit-plugins-groq>=1.6.0       # Groq Whisper STT + LLM
livekit-plugins-openai>=1.6.0     # Required by livekit-agents framework
livekit-api>=0.8.0                # LiveKit REST API (SIP trunk setup)

# ── LLM ───────────────────────────────────────────────────────────────────────
groq>=0.9.0                        # Groq Python client (Llama 3.3 70B)

# ── STT ───────────────────────────────────────────────────────────────────────
aiohttp>=3.9.0                     # Sarvam STT API (saarika:v2.5)
faster-whisper>=1.0.0              # Local fallback STT (CPU, no API key)

# ── TTS ───────────────────────────────────────────────────────────────────────
edge-tts>=6.1.0                    # Microsoft Edge TTS fallback (free)
soundfile>=0.12.0                  # Decode MP3/WAV audio
numpy>=1.24.0                      # PCM float→int16 conversion

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0               # .env loading
httpx>=0.27.0                      # HTTP client
